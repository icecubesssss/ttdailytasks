Drop your premium UI audio files here:

- `click.mp3` - navigation clicks
- `complete.mp3` - task completion chime
- `focus-start.mp3` - countdown start

The app already has remote fallback sounds, so it still works if these files are missing.
